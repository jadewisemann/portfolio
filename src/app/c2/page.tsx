import { Instrument } from "@/components/candidates/Instrument";

/**
 * /c2 — 기계 (INSTRUMENT). STRUCTURAL_BRANCH 후보. 첫 뷰포트 하나만 렌더한다.
 */
export default function C2Page() {
  return <Instrument />;
}
